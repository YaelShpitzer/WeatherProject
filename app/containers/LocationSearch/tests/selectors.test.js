// import { selectLocationSearchDomain } from '../selectors';

describe('selectLocationSearchDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
